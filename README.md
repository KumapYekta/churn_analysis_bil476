Customer Churn Analysis

This project predicts telecom customer churn using several machine learning models.

Make sure `telco_customer_churn.csv` is in the same folder, then run:

python churn_hyperparameter.py

After running, all figures will be saved in `figures_ieee/` and results in `results/`.